{
  "enabled": false,
  "_comment": "Opt-in bias reporting, off by default. You should not need to edit this or run any command: each agent asks once per session, before it starts work, and records your answer here itself. `session` is the session last asked; a new session gets asked again. When enabled, agents append a Context & Influence section to their reports and log each run to bias/log.jsonl. See bias/README.md."
}
