# Data Discovery Report — <TOPIC>

**Requested by:** <name> · **Date:** <YYYY-MM-DD> · **Run:** <run-id>

**Goal as understood:**
> <restate the researcher's goal in one or two sentences. If you narrowed or
> interpreted anything, say so here — this is what the manifest below answers.>

**Scope searched:** collection `<name>` · roots `<paths>` · types `<groups>`
**Corpus:** <N> files indexed · <N> matched by CSV · <N> inferred · <N> unattributed

---

## 1. Recommended datasets

Ordered by fit. Grouped by sequencing request, not by file.

### <MOLNG-####> — <Project Type>
**Fit:** <one line: why this serves the goal>
**Project:** <Scientific Projects> · **Requester:** <name> · **Status:** <status>
**Platform:** <Machine Type>, <Read Type> <Read Length>
**Original goal on file:** <Analysis Goals, trimmed>

| path | role | size |
| --- | --- | --- |
| `<representative path>` | raw reads | 8.2 GB |
| `<representative path>` | filtered count matrix | 410 MB |

<Shown N of M files. Full listing in `knowledge/ASSOCIATIONS.md`.>

> Repeat per dataset. Three to six is usually right; more becomes a list nobody
> reads.

---

## 2. Possibly relevant — inferred metadata only

These matched on folder naming, not on a request record. **Descriptions are
inferred and unverified — confirm before relying on them.**

| path | inferred as | confidence |
| --- | --- | --- |
| `/lab_data/reanalysis/sc_OE_h19/` | single-cell, olfactory epithelium, H19 | 0.60 |

---

## 3. What these data cannot answer

The substance of the report. Keep the four failure modes separate — each has a
different remedy.

### 3a. Unanswerable with any data in this collection
- **<aspect of the goal>** — <what is missing and why it blocks the question>.
  *Requires:* <the specific new experiment, timepoint, or assay>.

### 3b. Requested but never delivered
- <N> requests in the metadata CSV have no result path recorded. Relevant here:
  <MOLNG-####> (<project type>) — <why it would have mattered>.

### 3c. Indexed but absent from the scanned scope
- <N> requests have a CSV path but no matching files under `<roots>`. These may
  be untransferred, or outside the folders searched. Worth checking before
  concluding the data does not exist.

### 3d. Present but undocumented
- <N> files (<size>) on the endpoint carry no attributable metadata — no MOLNG
  anchor, and folder names too vague to interpret. Largest clusters:
  `<path>` (<N> files). These may contain relevant data; nothing can currently
  say so.

### Standing limitations
- `Analysis Result Paths` is unpopulated for all 70 requests, so no derived
  analysis output can be matched by request ID. Processed results reach the
  inferred tier at best.
- All 70 requests are from one lab; no cross-lab comparison is supported.
- <any paths that returned permission errors during the scan>

---

## 4. Suggested next steps
- <concrete action, e.g. "confirm whether MOLNG-#### was transferred">
- <concrete action, e.g. "add a README to /lab_data/new_data/ — 9 files unattributable">

---

*Generated by the Globus discovery agent. Associations recorded in
`knowledge/file_associations.jsonl`; this run logged in `knowledge/query_log.jsonl`.
Corrections welcome — they become permanent overrides.*
