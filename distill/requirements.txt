# Research agent -- the AGENT's own tooling only.
#
#   python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
#
# Markdown -> HTML for the PDF renderer. PDF generation itself uses headless
# Chrome (or Chromium/Edge/Brave) with a macOS cupsfilter fallback, so there is
# no LaTeX/wkhtmltopdf/weasyprint dependency to install.
markdown>=3.5
